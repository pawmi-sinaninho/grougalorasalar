# PHASE 7B-R — BOUNDARY REFINEMENT TEST REPORT

**Version:** 0.9.0  
**Status:** deterministic refinement implemented; final boundary acceptance blocked by seven unresolved cells

## Structural and authority result

| Check | Result |
|---|---:|
| Unique working coordinates | 338 |
| `x + y` / `x - y` count totals | 338 / 338 |
| Stable interior grid | PASS |
| Boundary cells / exposed edges | 50 / 104 |
| Confirmed / inferred / unresolved | 43 / 0 / 7 |
| Position outliers above `0.12` cell | 0 |
| Evidence review entries | 7 |
| Schema validation | PASS |
| Deterministic JSON/image regeneration | PASS |
| Boundary phase accepted | **NO** |

Unresolved cells:

```text
C009 (-2,-9)
C016 (-3,-8)
C025 (-4,-7)
C064 (-7,-4)
C081 (-8,-3)
C168 (13,0)
C193 (12,1)
```

Each has only `parity_geometric_consistency`; the unavailable green annotation is not counted. The cells are shown with orange geometric fill plus magenta unresolved marking.

## Position measurement

Every boundary entry records its expected empty-map centre, plausible centre from the named regional visible-grid line fit, pixel/cell distance, independent visible-surface flag and `0.12`-cell outlier result.

Seven regions fit at zero translation; `bottom-middle` fits at `(+0.5, 0.0)` px (`0.01` cell). No position outlier is present. This does not override the evidence gate for cells whose surface is hidden or visually implausible.

## Runtime result

- registration/recognition project all 338 candidates;
- solver-facing `walkable` contains 331 cells;
- seven cells are exposed as `boundaryUnverified`;
- no unresolved cell is silently accepted as walkable.

## Review artefacts

```text
assets/arena/grougalorasalar-boundary-debug.png
assets/arena/grougalorasalar-debug-overlay.png
VALIDATION/edge-review.overlay.png
VALIDATION/edge-review-report.json
VALIDATION/edge-review-zooms.png
```

The full report and all eight named crops are deterministically generated by `scripts/build_canonical_arena.py`.

## Remaining limit

No 100% correctness claim is allowed until the seven-cell review list is empty. The preferred evidence is the byte-original green annotation or exact manual centre confirmation for those cells.
